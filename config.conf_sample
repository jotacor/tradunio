# Rename to config.conf

[comunio]
user = user
passwd = passwd
user_id = user_id
community_id = community_id

[db_tradunio]
user = tradunio
passwd = passwd
host = localhost
db = tradunio
port = 3306
charset = utf8
